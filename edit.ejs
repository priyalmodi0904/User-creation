<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
   <div class="w-full h-full bg-zinc-900 text-white">
    <div class="nav mb-10">
        <a class="text-blue-500"href="/read">Read Users</a>
    </div>
    <h1 class="text-3xl tracking-tighter mb-3">Update User</h1>
   <form action="/update/<%= users._id  %>" method="post">
    <input class="px-5 py-3 bg-transparent border-2 border-white rounded-lg outline-none " type="text"name="name"  placeholder="name" value="<%= users.name %> ">
    <input class="px-5 py-3 bg-transparent border-2 border-white rounded-lg outline-none" type="text" name="email" placeholder="email" vlaue="<%= users.email  %>">
    <input class="px-5 py-3 bg-transparent border-2 border-white rounded-lg outline-none" type="text" name="image" placeholder="image url" value="<%= users.image  %>">
    <input class="px-5 py-3 bg-yellow-500 text-black rounded-lg m1-3" type="submit" value="Update User">
   </form>
</div>
</body>
</html>